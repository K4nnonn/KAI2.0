{
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["*.{css,js,json,png,jpg,svg,ico,woff,woff2}"]
  },
  "routes": [
    {
      "route": "/*",
      "allowedRoles": ["anonymous"]
    }
  ]
}
